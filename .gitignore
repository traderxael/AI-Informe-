__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store

# Node / Vercel / Build artifacts
node_modules/
node_modules.lock
.node_modules.lock
# package-lock.json SI se versiona (lo necesita `npm ci` en el gate de
# typecheck/tests del workflow). No ignorarlo: sin lockfile en el repo,
# npm ci falla y el gate del frontend no puede correr.
.vercel/
dist/
build/
*.log

# Grok / AI tooling
.grok/

# Environment secrets
scripts/.env
*.env
.env.local
.env.*.local
social_sources.json
docker/rsshub/docker-compose.local.yml
.vercel
